const WeatherComponent = (weather) => {
    date = weather.sys.dt_txt.split(" ")
    return `
        <section class="weather_component">
            
        </section>
    `
}

export default NoteHTML